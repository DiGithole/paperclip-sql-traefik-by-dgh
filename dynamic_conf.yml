http:
  routers:
    paperclip:
      rule: "PathPrefix(`/`)"
      service: paperclip_app
      entryPoints:
        - web

  services:
    paperclip_app:
      loadBalancer:
        servers:
          - url: "http://paperclip_app:3100"